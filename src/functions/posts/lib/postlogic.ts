import { NodeCallback } from "src/shared/model/nodecallback";

export class PostLogic {
    public static handle = async (event: object, context: object, callback: NodeCallback): Promise<void> => {

    }
}